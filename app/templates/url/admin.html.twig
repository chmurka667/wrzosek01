{% extends 'base.html.twig' %}

{% block title %}Create User{% endblock %}

{% block body %}
    <h1>label.administrator_edit</h1>

    {{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password) }}

    <button class="btn btn-primary" type="submit">Create</button>
    {{ form_end(form) }}
{% endblock %}
